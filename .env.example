CLIPSYNC_USERNAME=alice
CLIPSYNC_PASSWORD=changeme-on-first-login-please
# Generate with: openssl rand -hex 32
CLIPSYNC_SECRET_KEY=

# Optional. Used for both the host-side port and the in-container listen port.
# Default is 8080.
# CLIPSYNC_PORT=8085
